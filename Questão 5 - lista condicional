#include <stdio.h>
#include <math.h>

int main() {
	int num;
	
	printf("Digite um numero:");
	scanf("%d",&num);
	
	if(num > 0)
	{
		printf("Quadrado:%d\n",pow(num,2));
		printf("Raiz:%.2f\n",sqrt(num));
	}
	
}
